name = "stm32f407disco_bsp_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
stm32f407disco_bsp = "*"

[[pins]]
stm32f407disco_bsp = { path = ".." }

[build-profiles]
stm32f407disco_bsp       = "validation"
stm32f407disco_bsp_tests = "validation"
